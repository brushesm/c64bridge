@AGENTS.md

## Claude Code

- Keep this file small and treat `AGENTS.md` as the shared cross-tool contract.
- Put Claude-specific reusable rules in `.claude/rules/` only.
- Use `.github/skills/*/SKILL.md` as the execution source of truth for every workflow.